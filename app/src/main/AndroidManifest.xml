<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpinnerActivity"></activity>
        <activity android:name=".RaitingBarActivity" />
        <activity android:name=".RadioButtonActivity" />
        <activity android:name=".ToastActivity" />
        <activity android:name=".CustomListActivity" />
        <activity android:name=".SimpleListActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".AudioActivity" />
        <activity android:name=".VideoViewActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".ImageViewActivity" />
        <activity android:name=".FormInfo" />
        <activity android:name=".connectThreeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>